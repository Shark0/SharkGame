syntax = "proto3";
package com.shark.game.service;

message StatusRequest {
  string token = 1;
}

message StatusResponse {
  int32 status = 1;
  string message = 2;
}

service ReadBlackGameStatusService {
  rpc start(StatusRequest) returns (stream StatusResponse);
}